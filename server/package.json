{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "signaling-server.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "PROD=true npm run build && node ./dist/server/signalling-server",
    "postinstall": "cd ../frontend && npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/simple-peer": "^9.6.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "simple-peer": "^9.11.0",
    "socket.io": "^4.1.2"
  },
  "devDependencies": {
    "typescript": "^4.3.2"
  }
}
